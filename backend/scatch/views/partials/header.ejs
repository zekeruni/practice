<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3">
        <h3 class="text-xl">Scatch</h3>
        <% var loggedIn = typeof loggedIn !== "undefined" ? loggedIn : true %>
        <% if(loggedIn) { %>
            <div class="flex gap-5">
                <a class="rounded-lg p-2" href="">Shop</a>
                <a class="rounded-lg p-2" href="/cart">Cart</a>
                <a class="rounded-lg p-2" href="">My Account</a>
                <a class="bg-red-600 rounded-lg text-white p-2 text-sm" href="/users/logout">Logout</a>
            </div>
        <% } %>
    </nav>