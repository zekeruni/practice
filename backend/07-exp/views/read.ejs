<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read</title>
    <link rel="stylesheet" href="../css/index.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-5">
            <a href="/" class="bg-blue-500 rounded-lg p-2">Go Back</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>
        <div class="users flex flex-wrap gap-4">
            <% users.forEach((user) => { %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg flex justify-center flex-col items-center">
                    <div class="w-full h-52 bg-zinc-700 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-contain" src="<%= user.img %>" alt="NO IMG AVAILABLE">
                    </div>
                    <h3 class="text-xl tracking-tighter mt-3"> <%= user.name %> </h3>
                    <h5 class="text-zinc-500"> <%= user.email %> </h5>
                    <div class="flex gap-3 mt-5 justify-center">
                        <a href="/edit/<%= user._id %>" class="bg-yellow-500 rounded-lg p-2">Edit User</a>
                        <a href="/delete/<%= user._id %>" class="bg-red-500 rounded-lg p-2">Delete User</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>